"use strict";

var error = require("./../../error");
var Util = require("./../../util");

var subscriptions = module.exports = {
    subscriptions: {}
};

(function() {
    this.list = function(options, callback) {
        var self = this;
        // TODO
    };

// list
// Returns subscription resources that match the API request criteria. Try it now.
// insert
// Adds a subscription for the authenticated user's channel. Try it now.
// delete

}).call(subscriptions.subscriptions);
